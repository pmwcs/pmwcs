import { h } from 'preact';
import { FoundationElement } from '@rmwc/base';
import { ListProps } from './list';
export declare const useListFoundation: (props: ListProps & React.HTMLProps<any>) => {
    rootEl: FoundationElement<any, HTMLElement>;
};
